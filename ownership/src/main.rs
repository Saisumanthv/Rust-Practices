fn main() {
    // // move trait happens here.. 

    // let vec1 = vec!["Hello".to_string()];
    // let vec2 = vec1;
    // println!("{:?}", vec1);

    // // But, we can do clone trait

    // let vec1 = vec!["Hello".to_string()];
    // let vec2 = vec1.clone();
    // println!("{:?}", vec1);
    // println!("{:?}", vec2);

    // // copy trait is automatically done on values stored in the heap..
}
